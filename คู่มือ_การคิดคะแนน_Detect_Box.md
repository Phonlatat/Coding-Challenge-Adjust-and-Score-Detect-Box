# คู่มือการคิดคะแนน Detect-Box (เวอร์ชันอ่านง่าย)

เอกสารนี้อธิบายว่าโค้ดใน `01_IDT_BagAIScore.ipynb` คิดคะแนน `Detect-Box` ยังไง แบบภาษาคนทั่วไป แต่ความหมายทางเทคนิคครบ

---

## 1) Detect-Box คือคะแนนอะไร

คะแนน `Detect-Box` ใช้ดูว่าโมเดลตรวจจับวัตถุได้ดีแค่ไหนในแต่ละรูป โดยดู 3 เรื่องหลัก:

- ทายคลาสถูกไหม (cat/dog/panda)
- จำนวนกรอบใกล้กับที่ควรมีไหม
- มีกรอบทายผิดคลาสเยอะไหม

พูดง่ายๆ: **ยิ่งทายถูกและมั่นใจสูง คะแนนยิ่งดี / ยิ่งทายผิด คะแนนยิ่งโดนหัก**

---

## 2) คำที่ต้องรู้ก่อน

- `expected_count` = รูปนี้ควรมีวัตถุกี่ตัว
- `conf_correct` = ค่า confidence ของกรอบที่ทายคลาสถูก
- `conf_wrong` = ค่า confidence ของกรอบที่ทายคลาสผิด
- `PENALTY_WRONG_PER_BOX = 3.0` = ทายผิด 1 กรอบ โดนหัก 3 คะแนน
- `IMAGE_SCORE_MAX = 100 / จำนวนรูปทดสอบ`

ตัวอย่าง: ถ้ามีรูปทดสอบ 6 รูป คะแนนเต็มต่อรูปจะเป็น `100/6 = 16.67`

---

## 3) วิธีคิดคะแนนต่อ 1 รูป (ทีละขั้น)

### ขั้นที่ 1: เลือกกรอบ "ถูกคลาส" ที่ดีที่สุด

- เอาเฉพาะกรอบที่ทายคลาสถูก เก็บใน `conf_correct`
- เรียงจาก confidence มากไปน้อย
- เลือกมาแค่ `k` ตัว โดย `k = expected_count`

เช่น รูปควรมี 2 ตัว ก็ใช้ top-2 ของกรอบที่ถูกคลาส

### ขั้นที่ 2: หาคะแนนพื้นฐานจากความมั่นใจ

- `base = ค่าเฉลี่ยของ confidence ใน top-k`

ถ้า confidence สูง `base` ก็สูง

### ขั้นที่ 3: ถ้าตีเกินจำนวนที่ควรมี จะลดคะแนน

- ถ้าจำนวนกรอบถูกคลาสที่ตรวจเจอจริง (`n_correct`) มากกว่า `k`
  - ใช้ตัวคูณลดคะแนน: `over_factor = k / n_correct`
- ถ้าไม่เกิน ใช้ `over_factor = 1`

เหตุผล: กันกรณีโมเดล "ยิงกรอบถูกคลาสเยอะเกินจริง"

### ขั้นที่ 4: คำนวณคะแนนดิบ

- `raw_score = IMAGE_SCORE_MAX * base * over_factor`

### ขั้นที่ 5: หักคะแนนกรอบผิดคลาส

- `wrong_penalty = 3.0 * จำนวนกรอบผิดคลาส`

### ขั้นที่ 6: ได้คะแนนสุดท้ายของรูป

- `image_score = max(0, raw_score - wrong_penalty)`

`max(0, ...)` หมายถึงคะแนนต่ำสุดคือ 0 ไม่มีติดลบ

---

## 4) คะแนนรวม Detect-Box

- เอาคะแนนทุกรูปรวมกัน:

`Detect-Box Total = sum(image_score ของทุกรูป)`

โครงนี้ตั้งใจให้คะแนนเต็มรวมประมาณ 100

---

## 5) คะแนนรวมสุดท้ายของงาน (ตามโค้ดปัจจุบัน)

ในโน้ตบุ๊กนี้ใช้สูตร:

`Total Score = mAP50 + mAP75 + Detect-Box`

ตัวอย่าง:

- `mAP50 = 98.72`
- `mAP75 = 84.25`
- `Detect-Box = 56.30`

ดังนั้น:

`Total Score = 98.72 + 84.25 + 56.30 = 239.27`

---

## 6) ทำไมบางรูปไม่มีกรอบเลย

ปัญหานี้เจอบ่อย และมักมาจาก:

- ตั้ง `conf` สูงเกินไป (กรอบที่มั่นใจไม่ถึงเลยถูกตัด)
- รูปยาก เช่น มืด เบลอ ไกล หรือวัตถุเล็ก
- ข้อมูลตอน train ยังไม่ครอบคลุมรูปแบบนั้น
- โหลดโมเดลคนละไฟล์กับที่เทรนไว้

### วิธีเช็คว่าเป็นเพราะ threshold ไหม

ลองรูปเดิม แล้วลด `conf` ทีละระดับ:

- `conf=0.50`
- `conf=0.25`
- `conf=0.10`
- `conf=0.05`

ถ้าลด `conf` แล้วเริ่มมีกรอบ แปลว่าเกี่ยวกับ threshold แน่นอน

---

## 7) ข้อแนะนำตอนใช้งานจริง

- ตอน debug ให้เริ่มด้วย `conf` ต่ำ เช่น `0.10` หรือ `0.05`
- ตอนใช้งานจริงค่อยเพิ่ม `conf` เพื่อกันกรอบมั่ว (false positive)
- ถ้า `conf=0.05` แล้วยังไม่เจอ ให้ปรับข้อมูล/train มากกว่าปรับ threshold อย่างเดียว

---

## 8) สรุปสั้นมาก

- ให้คะแนนจากกรอบที่ "ถูกคลาสและมั่นใจสูง"
- หักคะแนนเมื่อทายผิดคลาส
- ตีเกินจำนวนที่ควรมีจะถูกลดคะแนนด้วย
- ค่า `conf` มีผลโดยตรงกับการที่รูปนั้นจะ "มีกรอบ" หรือ "ไม่มีกรอบ"

